# Writing archive integration

All 29 revised articles are in `content/writing/` as Markdown bodies and JSON metadata. The original frontmatter values and tags are preserved. MDX was converted without changing article bodies; the source files use plain Markdown without custom components.

At Nathan’s request, `date` is displayed as the publication date and used for publication metadata. `writtenOn` remains internal drafting provenance. Neither perspective labels nor drafting dates are shown on the site. Tags are visible and clickable; the index provides search and five-article pagination.

`manifest.json` retains the source archive's historical checks, opinion continuity, and editorial provenance, with paths updated for this repository. Earlier draft review notes are retained as history; Nathan authorized integrating all 29 articles in this task.
